module.exports = {
    purge: ["./src/**/*.{js,jsx,ts,tsx}", "./public/index.html"],
    darkMode: false, // 'media' or 'class' for dark mode configuration
    theme: {
        extend: {},
    },
    variants: {
        extend: {},
    },
    plugins: [],
};